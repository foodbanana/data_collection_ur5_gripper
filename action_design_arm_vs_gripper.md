# action 설계: 팔 ≠ 그리퍼 (되돌리기 전에 꼭 읽을 것)

> **한 줄 요약**: action 벡터에서 팔 6축은 `states[i+1]`(다음 프레임 상태)로 유도하고,
> 그리퍼 1축은 `target[i]`(그 시점 goal 명령)를 그대로 쓴다. 둘은 **일부러** 다르게 처리한다.
> 통일하면 그리퍼 파지 신호가 사라진다.

---

## 결론

Stage 1(`lerobot_stage1_extract_bag.py`)에서 만드는 `actions.npy`의 각 프레임은:

```
action[i] = [ 팔 6축 = states[i+1][:6] ,  그리퍼 = target[i] ]
             └───────── 유도 ─────────┘   └──── 그대로 ────┘
```

- **팔(0:6)** = 다음 프레임의 관절 상태 (`states[i+1]`)
- **그리퍼(6)** = 그 시점 `/gripper/target`의 goal 값 (`target[i]`, raw {0, 1150})

이 둘을 통일하지 말 것. (예: "그리퍼도 `states[i+1]`로" 또는 "팔도 명령 토픽으로")

---

## 왜 다른가 — action을 "얻는 방법"이 다르기 때문

action은 "프레임 i에서 로봇에게 내린 명령"이다. 문제는 **팔과 그리퍼 중 한쪽만
실제 명령 기록이 있다**는 것이다.

```
┌─────────────────────── action 생성 (팔 ≠ 그리퍼) ───────────────────────┐
│                                                                          │
│   프레임 i 의 action[7] =                                                │
│                                                                          │
│     [0:6] 팔 관절 6축          [6] 그리퍼 1축                            │
│      ────────────────          ─────────────                            │
│      = states[i+1] 의 팔       = target[i]  (그 시점 goal 명령)          │
│                                                                          │
│      ▲ freedrive 시연이라       ▲ teleop 이 goal 을 실제로 명령했고      │
│        "명령 신호"가 없음  →      /gripper/target 에 기록됨  →            │
│        다음 프레임 상태를          그대로 action 으로 사용                │
│        목표로 유도                                                       │
│                                                                          │
│   왜 갈리나: action 을 "얻는 방법"이 다름                                │
│     · 팔    = 명령 기록 없음 → 상태 변화로 역산 (states[i+1])            │
│     · 그리퍼 = 명령 기록 있음 → 그대로 사용 (target[i])                  │
│                                                                          │
│   ┌────────────────── 그리퍼에서 이게 왜 중요한가 ──────────────────┐   │
│   │  물체 파지 시:                                                  │   │
│   │    action(goal)   = 1150   "꽉 닫아라"     ← 학습 목표          │   │
│   │    state(present) =  742   물체에 막혀 멈춤 ← 실제 파지 깊이    │   │
│   │                                                                │   │
│   │  만약 그리퍼도 states[i+1] 을 썼다면 →                         │   │
│   │    action = 742 "742까지만 닫아라" → 약하게 쥠 (파지 신호 소실) │   │
│   └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────┘
```

### 팔 — 명령 기록이 없음 → 유도

팔은 **freedrive**로 시연한다. 사람이 손으로 팔을 직접 끌어 움직이므로,
"관절을 여기로 가라"는 명령 신호 자체가 존재하지 않는다. 그래서 팔의 action은
**"다음 순간 팔이 실제로 간 위치"를 목표였다고 간주**해서 `states[i+1]`로 유도한다.

### 그리퍼 — 명령 기록이 있음 → 그대로

그리퍼는 teleop으로 goal을 명령하고, 그 값이 `/gripper/target`에 그대로 기록된다.
프레임 i의 명령이 이미 `target[i]`에 있으니 유도할 필요 없이 그대로 쓴다.

---

## 그리퍼에서 이게 왜 결정적인가 — 파지 신호

핵심은 **obs와 action의 그리퍼 소스가 다르다**는 점이다.

| | 소스 | 값 | 물체 파지 시 |
|---|---|---|---|
| obs (state[6]) | present position (실측) | 연속 0~1150 | **742** (물체에 막혀 멈춤) |
| action[6] | `/gripper/target` (goal) | 이진 {0, 1150} | **1150** ("꽉 닫아라") |

물체를 잡으면 goal은 1150(완전 닫힘)을 명령하지만, present는 물체에 막혀
중간값(예: 742)에서 멈춘다. 이 **1150 vs 742의 벌어짐이 "물체를 잡고 있다"는 신호**다.

- **action = goal(1150)** → 모델은 "확실히 꽉 닫아라"를 학습 → 추론 때 힘 상한
  (Goal Current 400mA)까지 조여 단단히 잡음
- 실제 파지 깊이(742)는 obs에 보존됨 → 모델이 파지 상태를 인식

### 만약 통일하면 (그리퍼도 `states[i+1]`로) — 하지 말 것

action = 742("742까지만 닫아라")가 되어, 모델이 **물체 앞에서 약하게 멈추는 것**을
학습한다. 파지 신호가 데이터에서 사라지고, 추론 때 물체를 놓치거나 헐겁게 쥔다.

---

## 실측 검증 (2026-09-21, fake 팔 + 실물 그리퍼)

테스트 에피소드에서 확인:

```
닫기 명령(action=1150) 프레임 수: 158
닫기 명령 중 state 분포 [min/25/50/75/max]: [0, 52, 136, 242, 741]
→ action=1150(꽉 닫아라)인데 state가 741에서 멈춤 (물체에 막힘)
```

action은 1150 고정, state는 0→52→136→242→741로 닫히다 741에서 정지.
설계대로 파지 신호가 데이터에 보존됨을 확인.

---

## 관련 위치

- 생성: `lerobot_stage1_extract_bag.py` (action 조립은 Stage 1에서 완성)
- 소비: `lerobot_stage2_build_dataset_v21.py` / `_v30.py` (`action_i = actions[i]`로 읽기만)
- 검수: `inspect_parquet.py` (action 이진 여부 + 파지 신호 표시)
- 그리퍼 소스: `rh_gripper_node.py` (present + target을 같은 stamp로 30Hz 발행)

## 시점 주의 (off-by-one)

- 팔: `states[i+1]` — 다음 프레임 (마지막 프레임은 `states[N-1]` 복제)
- 그리퍼: `target[i]` — 현재 프레임 (미래 아님). 추론 때 정책 출력을 그대로
  `/gripper/command`로 보내는 구조와 일치.
